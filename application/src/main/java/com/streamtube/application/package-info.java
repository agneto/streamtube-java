/**
 * Application layer — use cases / interactors that orchestrate domain ports.
 *
 * <p>Depends only on {@code domain}. Defines input/output boundaries (ports) that the
 * infrastructure layer implements. Use cases (registration, upload initiation, etc.)
 * are introduced from Phase 02 on.
 */
package com.streamtube.application;
