/**
 * Domain layer — entities, value objects, domain services and repository ports.
 *
 * <p>This layer is framework-free: no Spring, no JPA, no web annotations. It is the
 * innermost circle of the Clean Architecture; nothing here depends on outer layers.
 * Business tables/entities (users, channels, videos) are introduced from Phase 02 on.
 */
package com.streamtube.domain;
