# Build performance: independent modules build in parallel and task outputs are cached.
# (Configuration cache intentionally off: the Spotless version in use is not compatible.)
org.gradle.parallel=true
org.gradle.caching=true
